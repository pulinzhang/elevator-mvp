# Cloudflare 本地开发环境变量
# 用于 next-on-pages 本地预览时的 D1 数据库绑定
# 
# 注意：本地开发时使用 better-sqlite3 (elevator.db)
# 只有在部署到 Cloudflare 后才会使用 D1

# 如果需要本地模拟 D1，可以运行:
# npx wrangler d1 create elevator-db --local
# 然后将返回的 database_id 填入下面

